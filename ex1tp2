#include <stdio.h>
#include <stdlib.h>

int main()

{
  int n ;

    printf("entre valeur n\n");
    scanf("%d",&n);


    if ((n%3)==0)
    {
         printf("n est divisible par 3 ");
    }
    else
    {
        printf("n ne pas divisible par 3");
    }
    return 0;
}
