#include <stdio.h>
#include <stdlib.h>
# include<math.h>

int main()
{
    double c;
    int z;

    printf("entre valeur c\n");
    scanf("%lf",&c);
    z=c;
    if(z<0)
    {
        z=-z;
    }
    printf (" sisaier absolue de c = %lf \n",abs(c) );

    return 0;
}
